# v1.0.5 Intelligent Retrieval Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把技能检索机制从单轮静态路由升级为五阶段检索循环（查询分解 / 意图澄清 / 首轮路由 / 证据评估 / 二轮定向检索），将 18 个 playbook 文件（24 个 playbook 小节）的 §3 线性步骤改为证据驱动决策表，并新增 Mesh Shader 与桌面窗口集成两张 API 专卡。

**Architecture:** 本仓库是 Markdown 知识库（无代码），"实现"= 新建/编辑 references/ 下的 .md 文件；"测试"= pytest 套件（tests/ 检查反引号 .md 引用可解析、模板结构）+ grep 结构验证 + `git diff --check`。三项智能能力内聚于一个协议文件 `progressive_retrieval.md`；决策树落点在 playbook 内容与模板，不新增规范文件。

**Tech Stack:** Markdown 知识文件、pytest（既有 64 用例）、git。

## Global Constraints

- git 跟踪文件最终数 = 199（当前 196 含设计文档，新增 3 个内容文件），**不得超过 200**（ClawHub 上限）。
- 所有新文件与编辑行使用 **LF 行尾**、无行尾空白（`git diff --check` 必须干净）。
- 反引号引用的 `.md` 路径必须能从所在文件目录解析（pytest test_backtick_md_references_exist 会检查）；引用 references 内文件时写模块相对路径（如 `00_expert_entry/progressive_retrieval.md`）。
- 所有 API 硬规则标注来源等级（`[SPEC]`/`[REGISTRY]`/`[REF]`/`[ANDROID]`/`[GUIDE]`/`[ENGINE]`/`[HEUR]` 等）；性能建议标注适用条件；Android 相关结论标注适用边界。
- 修复分级命名只用 Workaround / Minimal Fix / Structural Fix；禁止"现象消失 = 根因修复"表述。
- API 卡片遵循 `03_api_manual/api_card_template.md` 的完整模板结构（§0-§15）。
- playbook §3 改造只替换"快速验证路径"小节内容为决策表；§2 假设表、§5 根因表、其余小节标题与内容不动（pytest test_templates 的 `## Debug Playbook:` 前缀与 `###` 段落校验必须继续通过）。
- 每个任务结束运行 `python -m pytest tests/ -q`（预期 64 passed, 1 skipped 或更多 passed）与 `git diff --check`（预期无输出）。
- 提交信息用英文，风格与仓库历史一致（`git log --oneline -5` 参考）。
- 设计文档 `docs/superpowers/specs/2026-08-29-v1.0.5-design.md` 中"22 个 playbook"为笔误，实际是 **18 个 playbook 文件 / 24 个 playbook 小节**；在 Task 2 中修正该文档。

---

### Task 1: 新建检索循环协议 `00_expert_entry/progressive_retrieval.md`

**Files:**
- Create: `references/00_expert_entry/progressive_retrieval.md`
- Modify: `references/00_expert_entry/README.md`（文件列表登记）

**Interfaces:**
- Produces: 五阶段循环 R1-R5 协议；后续任务引用它时统一写 `00_expert_entry/progressive_retrieval.md`；阶段名固定为 R1 查询分解 / R2 意图澄清 / R3 首轮路由 / R4 证据评估 / R5 二轮定向检索。

- [ ] **Step 1: 创建 `progressive_retrieval.md`，完整内容如下**

```md
# Progressive Retrieval（检索循环协议）

本文件把"单次静态路由"升级为五阶段检索循环。三项能力——查询分解、意图澄清、证据评估——是同一个循环的不同阶段，统一在本文件定义。

适用判断：

- 问题简单、单类型、信息完整（如"VkSemaphore 和 VkFence 的区别"）→ 走 `07_integration_pack/task_routing_rules.md` 单轮路由即可，不进入循环。
- 问题复杂、跨类型、模糊、或首轮回答后仍存在低置信子问题 → 进入本循环。

---

## R1 查询分解

复杂问题先拆为子问题，再逐个路由。拆解维度：

1. **对象链路维度**：问题涉及哪些 Vulkan 对象链（Surface/Swapchain、Descriptor、Pipeline、同步、Command Buffer…）。
2. **平台维度**：Android / Desktop / 通用；Android 必须单列子问题（Surface 生命周期、rotation、pause/resume）。
3. **症状维度**：黑屏 / crash / hang / Validation Error / 性能 / 无输出。

规则：

- 每个子问题标记目标模块（playbook / workflow / API card / case）。
- 跨类型任务（如"黑屏 + Android + 卡顿"）必须拆解，拆解后与 `00_expert_entry/task_classifier.md` 的分类逐一对应。
- 子问题数量 ≤4；超过说明问题过大，先与用户确认范围。

## R2 意图澄清

信息不足时**先问再查**，不要基于猜测路由。触发判据（满足任一即触发）：

- 缺平台：不知道 Android / Desktop / 版本。
- 缺现象细节："画面不对"级别的模糊描述，无法区分黑屏 / 闪烁 / 花屏 / 拉伸。
- 缺报错证据：无 Validation message / VUID / logcat / 返回值。
- 假设冲突：多个候选根因指向相反的排查方向（如 cull 假设要求关 depth test，depth 假设要求关 cull）。

最小提问清单规则：

- 每项都必须是"补齐即可完成路由"的证据，不问与路由无关的问题。
- 一次 ≤3 项；超出时按"对剪枝贡献最大"排序取前 3。
- 输出格式见 `00_expert_entry/response_formats.md` §0.5 意图澄清。
- 用户拒绝补充或确实无法提供 → 按 HEUR 降级路由，回答中显式标注假设。

## R3 首轮路由

循环入口即现有路由，保持不变：

- 路由表：`07_integration_pack/task_routing_rules.md`（9 类任务）。
- 检索策略与最小文件上限：`07_integration_pack/retrieval_policy.md` §4（1 workflow + 1 playbook + 1-3 API card + 0-2 case）。

首轮对**每个子问题**分别路由，不合并加载。

## R4 证据评估

回答前自检。对每个子问题标注置信度：

- **高**：已加载的资料直接覆盖该子问题（playbook §2 假设表 / API 卡 §7 高频错误 / case 根因与之匹配）。
- **中**：资料部分覆盖，需要工程推断补足。
- **低**：资料未覆盖（资料缺口）或与已知证据冲突。

规则：

- 任一子问题为低置信 → 必须执行 R5，不允许带低置信直接回答。
- 证据清单（本轮加载了什么、确认了什么）必须保留，作为 Verification Gate 的输入。

## R5 二轮定向检索

只对低置信子问题定向补载：

- playbook §3 决策表指向的 API 卡片 / case（决策表格式见 `04_debug_playbooks/debug_playbook_template.md` §3）。
- 补载后回到 R4 重新评估；循环 ≤2 轮。

终止条件（满足其一）：

1. 全部子问题高置信。
2. 触及 `07_integration_pack/retrieval_policy.md` §4 最小文件上限。
3. 循环满 2 轮仍有低置信 → 回答中显式标注"未验证关卡 + 原因"，交由 Verification Gate 处理。

---

## 与 Verification Gate 的边界

- 本循环管"回答前找齐证据"；`00_expert_entry/verification_gate.md` 管"结论前过 G1-G6"。
- 循环的输出（证据清单 + 各子问题置信度）是 Gate 的输入；二者串联，不重叠。

## 决策摘要

| 情形 | 动作 |
|---|---|
| 简单单类型问题 | 单轮路由，不进循环 |
| 跨类型 / 复杂问题 | R1 分解 → R3 逐子问题路由 → R4 评估 |
| 信息不足（触发 R2 判据） | 先给最小提问清单（≤3 项），再路由 |
| 存在低置信子问题 | R5 定向补载，回到 R4，≤2 轮 |
| 2 轮后仍低置信 | 显式标注未验证关卡，交 Verification Gate |
```

- [ ] **Step 2: 在 `references/00_expert_entry/README.md` 的文件列表中登记（在 verification_gate.md 条目之后加一行）**

```md
- `progressive_retrieval.md` — 检索循环协议：查询分解、意图澄清、证据评估与二轮定向检索
```

- [ ] **Step 3: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add references/00_expert_entry/progressive_retrieval.md references/00_expert_entry/README.md
git commit -m "Add progressive retrieval loop protocol (R1-R5)"
```

---

### Task 2: 模板 §3 决策表化 + 修正设计文档笔误

**Files:**
- Modify: `references/04_debug_playbooks/debug_playbook_template.md`（§3 小节）
- Modify: `references/04_debug_playbooks/README.md`（规范说明）
- Modify: `docs/superpowers/specs/2026-08-29-v1.0.5-design.md`（"22 个 playbook"→"18 个 playbook 文件 / 24 个 playbook 小节"，两处）

**Interfaces:**
- Produces: §3 决策表的标准列定义 `| # | 检查（成本升序） | 结果 A → 下一步 | 结果 B → 下一步 | 剪枝（排除的假设） |`；Task 3-5 的 24 个小节全部复用此列结构；"下一步"指向本 playbook 内小节（§5 根因表 / §6 修复 / 其他检查行）或跨文件相对路径。

- [ ] **Step 1: 替换 `debug_playbook_template.md` §3 小节**

将现有 §3：

```md
## 3. 快速验证路径

优先使用低成本验证方式：

1. log / return code
2. Validation Layer
3. clear color / debug shader
4. RenderDoc / AGI
5. 最小复现
```

替换为：

```md
## 3. 快速验证路径（证据驱动决策表）

按成本升序逐行检查；每行依据结果分支，并剪枝（排除）§2 中对应优先级的假设。不按固定顺序执行全部步骤，而是随证据走分支。

| # | 检查（成本升序） | 结果 A → 下一步 | 结果 B → 下一步 | 剪枝（排除的假设） |
|---|---|---|---|---|
| 1 | log / return code | 有错误码 → 按 §5 根因表定位 | 无输出 → 检查 2 | — |
| 2 | Validation Layer | 有 VUID → §5 根因表定位 | clean → 检查 3 | — |
| 3 | RenderDoc：draw/dispatch 是否提交 | 无 → 提交链路（§4 CommandBuffer/Queue Submit） | 有 → 检查 4 | 排除 §2 的 shader/资源假设 |
| 4 | clear color / debug shader | 不可见 → swapchain/present 分支 | 可见 → 检查 5 | 排除 §2 的提交链路假设 |
| 5 | 最小复现 | 复现 → 收敛根因（§5） | 不复现 → §11 不确定处理 | — |

规则：

- 剪枝列必须引用 §2 假设表的优先级编号（P0/P1/P2）。
- 每个具体 playbook 依据自身 §2 / §5 定制行；上表为骨架示例。
- 检查成本排序：log < Validation < RenderDoc/AGI capture < 最小复现。
```

- [ ] **Step 2: 在 `04_debug_playbooks/README.md` 第 4 节规则列表（"10. 禁止把…"条目之后）追加一条**

```md
11. §3 快速验证路径必须为证据驱动决策表：按成本升序检查、依据结果分支、剪枝列引用 §2 假设编号；不写线性步骤清单。
```

- [ ] **Step 3: 修正设计文档两处"22 个 playbook"为"18 个 playbook 文件 / 24 个 playbook 小节"**

`docs/superpowers/specs/2026-08-29-v1.0.5-design.md` 中 §4.1 出现两次"22 个 playbook"（改造范围、验收标准 3），全部替换为"18 个 playbook 文件的 24 个小节"。

- [ ] **Step 4: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 5: Commit**

```bash
git add references/04_debug_playbooks/debug_playbook_template.md references/04_debug_playbooks/README.md docs/superpowers/specs/2026-08-29-v1.0.5-design.md
git commit -m "Convert playbook template section 3 to evidence-driven decision table"
```

---

### Task 3: playbook §3 决策表化——批次 1（visual / crash / validation，10 文件 13 小节）

**Files:**
- Modify: `references/04_debug_playbooks/01_visual_issues/black_screen.md`、`depth_test_wrong.md`、`flickering.md`
- Modify: `references/04_debug_playbooks/02_crash_hang/device_lost.md`、`gpu_hang.md`、`swapchain_recreate_crash.md`
- Modify: `references/04_debug_playbooks/03_validation_errors/descriptor_pipeline_layout_errors.md`（2 小节）、`layout_sync_hazard_errors.md`（2 小节）、`memory_leak.md`、`validation_error_decode.md`

**Interfaces:**
- Consumes: Task 2 的决策表列结构与规则（剪枝列引用本文件 §2 假设编号）。
- Produces: 无（终端改造）。

- [ ] **Step 1: 对每个 playbook 的每个 `## 3.` 小节执行转换算法**

算法（对每个小节执行）：

1. 读取本 playbook §2 假设表（P0/P1/P2 各假设）与 §5 高频根因。
2. 保留"低成本检查在前"的成本排序：log/return code → Validation → RenderDoc/AGI → 最小复现/代码检查。
3. 每行写：检查项（具体到该 playbook 的领域，如 black_screen 写"RenderDoc: Primitives culled 计数"）→ 结果 A/B 分支（指向 §5 根因条目、§6 修复、或下一检查行）→ 剪枝列引用 §2 对应 P 编号。
4. 删除原线性编号列表，替换为决策表；小节标题保持原有层级，仅追加后缀：单 playbook 文件为 `## 3. 快速验证路径（证据驱动决策表）`，合并文件（含 2 个 playbook）内为 `### 3. 快速验证路径（证据驱动决策表）`。

black_screen.md 的完整示例（其余 playbook 按同一结构定制）：

```md
## 3. 快速验证路径（证据驱动决策表）

| # | 检查（成本升序） | 结果 A → 下一步 | 结果 B → 下一步 | 剪枝（排除的假设） |
|---|---|---|---|---|
| 1 | Validation Layer | 有 VUID → §5 对应根因 | clean → 检查 2 | — |
| 2 | RenderDoc：draw call 是否提交 | 无 draw → §4 提交链路排查 | 有 draw → 检查 3 | 排除 §2 的 descriptor/pipeline 配置假设（无提交则未走到绑定） |
| 3 | Primitives culled 计数 | = 输入数 → cull/winding 分支（§5） | 正常 → 检查 4 | 排除 §2 的 P0 cull/frontFace 假设 |
| 4 | Fragments passing depth test | = 0 → depth 分支（§5 compare op / clear value） | > 0 → 检查 5 | 排除 §2 的 depth 假设 |
| 5 | clear color 是否可见 | 不可见 → present/swapchain 分支（§4） | 可见 → §11 不确定处理 | 排除 §2 的提交链路假设 |
```

各 playbook 定制要点（行内容依据其 §2/§5）：

| Playbook | 决策表关键行（除通用前两行外） |
|---|---|
| black_screen | culled 计数 → depth passing → clear color（见上例） |
| depth_test_wrong | depthCompareOp vs clip-space Z 范围 → clear value vs compare op → reverse-Z 判定 |
| flickering | 帧间差异是否周期性（frame-in-flight 复用）→ acquire/present semaphore 复用 → barrier 覆盖检查 |
| device_lost | 最后一次成功 submit → Validation/日志中首个 VUID → 驱动返回码（VK_ERROR_DEVICE_LOST 位置：acquire/submit/present） |
| gpu_hang | 是否可复现（固定场景）→ 该场景 pass 类型（compute/graphics）→ AGI 最后完成的 pass → 死循环 shader / 未同步的间接依赖 |
| swapchain_recreate_crash | crash 时机（recreate 中/后首帧）→ 旧 image view 引用检查 → in-flight command buffer 引用旧 swapchain |
| descriptor_pipeline_layout_errors（2 小节） | VUID 属于 layout 不匹配还是类型不匹配 → shader reflection 结果比对 → pipeline 是否用旧 layout 创建 |
| layout_sync_hazard_errors（2 小节） | VUID 属于 hazard 还是 layout mismatch → barrier 的 producer/consumer stage → descriptor imageLayout vs 实际 layout |
| memory_leak | 对象计数趋势（每帧增长 vs 一次性）→ 释放路径断点/日志 → GPU 仍在使用即销毁（fence 未等待） |
| validation_error_decode | VUID 解码（对象/函数/约束）→ 消息中 object handle 定位创建点 → 沿 §4 对象链路反查使用点 |

- [ ] **Step 2: 结构验证**

Run: `git grep -c "快速验证路径（证据驱动决策表）" -- references/04_debug_playbooks/01_visual_issues references/04_debug_playbooks/02_crash_hang references/04_debug_playbooks/03_validation_errors`
Expected: 13 行（每个小节一行；10 个文件，其中 2 个文件各有 2 个小节）

Run: `git grep -n "3\. 快速验证路径$" -- references/04_debug_playbooks/01_visual_issues references/04_debug_playbooks/02_crash_hang references/04_debug_playbooks/03_validation_errors`
Expected: 0（旧标题无残留；匹配任意层级）

- [ ] **Step 3: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add references/04_debug_playbooks/01_visual_issues references/04_debug_playbooks/02_crash_hang references/04_debug_playbooks/03_validation_errors
git commit -m "Convert visual/crash/validation playbook section 3 to decision tables (13 playbooks)"
```

---

### Task 4: playbook §3 决策表化——批次 2（resource_sync / android，3 文件 3 小节）

**Files:**
- Modify: `references/04_debug_playbooks/04_resource_sync/compute_graphics_sync_error.md`、`compute_no_output.md`
- Modify: `references/04_debug_playbooks/05_android_specific/android_surface_lifecycle.md`

**Interfaces:**
- Consumes: Task 2 的决策表列结构与规则。
- Produces: 无。

- [ ] **Step 1: 按 Task 3 Step 1 的算法转换 3 个小节**

定制要点：

| Playbook | 决策表关键行（除通用前两行外） |
|---|---|
| compute_graphics_sync_error | compute dispatch 是否执行（AGI）→ barrier 是否覆盖 compute→graphics（producer stage=COMPUTE，consumer stage=FRAGMENT）→ image layout 是否 transition 到读取侧所需 |
| compute_no_output | groupCount 各维 > 0 断言 → storage image/buffer 是否被后续 pass 读取（读取时机早于写入完成）→ dispatch 参数 domain 与资源尺寸匹配 |
| android_surface_lifecycle | logcat surface destroyed/changed 时机 vs crash 时机 → pause/resume 后 render thread 状态 → swapchain 重建是否覆盖全部依赖资源组 |

- [ ] **Step 2: 结构验证**

Run: `git grep -c "快速验证路径（证据驱动决策表）" -- references/04_debug_playbooks/04_resource_sync references/04_debug_playbooks/05_android_specific`
Expected: 3 行

- [ ] **Step 3: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add references/04_debug_playbooks/04_resource_sync references/04_debug_playbooks/05_android_specific
git commit -m "Convert resource sync and android playbook section 3 to decision tables"
```

---

### Task 5: playbook §3 决策表化——批次 3（performance，5 文件 8 小节）

**Files:**
- Modify: `references/04_debug_playbooks/06_performance_symptoms/gpu_frame_time_high.md`
- Modify: `references/04_debug_playbooks/06_performance_symptoms/bandwidth_fullscreen_cost.md`（2 小节）
- Modify: `references/04_debug_playbooks/06_performance_symptoms/barrier_draw_call_stall.md`（2 小节）
- Modify: `references/04_debug_playbooks/06_performance_symptoms/cpu_overhead_symptoms.md`（2 小节）
- Modify: `references/04_debug_playbooks/06_performance_symptoms/pipeline_startup_stutter.md`（2 小节）

**Interfaces:**
- Consumes: Task 2 的决策表列结构与规则。
- Produces: 无。

- [ ] **Step 1: 按同一算法转换 8 个小节**

定制要点（性能类首行是瓶颈分类而非 log，成本排序：计时/计数器 < trace < 对照实验）：

| Playbook | 决策表关键行 |
|---|---|
| gpu_frame_time_high | AGI GPU vs CPU 帧时间（定 GPU/CPU 瓶颈）→ 分 pass timer query 定位热点 pass → 热点类型（fragment/vertex/bandwidth）→ early-z 与 overdraw 计数 |
| bandwidth_fullscreen_cost（Bandwidth High） | AGI bandwidth counter 定读写大户（attachment vs texture）→ 中间 attachment 数量 → 是否可压缩格式/降精度 |
| bandwidth_fullscreen_cost（Fullscreen Cost） | 该 pass 单独计时（跳过对照）→ 采样次数/中间 attachment → 半分辨率可行性对照 |
| barrier_draw_call_stall（Barrier Overhead） | AGI timeline idle bubble 定位 → barrier stage/access 粒度检查（是否 ALL_COMMANDS）→ 移除实验（安全收窄后对照） |
| barrier_draw_call_stall（Draw Call Bottleneck） | CPU submit 耗时 vs GPU（定 CPU 瓶颈）→ draw call 计数与材质分布 → instancing/合并可行性 |
| cpu_overhead_symptoms（CPU Frame Time High） | 分类计时：录制/提交/等待 → descriptor 更新次数 → pipeline 编译次数（首次 vs 稳态） |
| cpu_overhead_symptoms（Descriptor Update Overhead） | update/allocate 调用计数 → 按频率分层现状 → push constant 可替代项 |
| pipeline_startup_stutter（Pipeline Creation Stutter） | 卡顿帧是否伴随 pipeline 创建日志 → cache 命中率 → 首次变体 vs 预热覆盖 |
| pipeline_startup_stutter（Startup Time High） | 分段计时：资源 IO / 编译 / pipeline 创建 → 首屏必需集 vs 全量加载 |

- [ ] **Step 2: 结构验证**

Run: `git grep -c "快速验证路径（证据驱动决策表）" -- references/04_debug_playbooks/06_performance_symptoms`
Expected: 8 行

Run: `git grep -c "快速验证路径（证据驱动决策表）" -- references/04_debug_playbooks/`
Expected: 25 行（24 个小节 + 模板 1）

- [ ] **Step 3: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add references/04_debug_playbooks/06_performance_symptoms
git commit -m "Convert performance playbook section 3 to decision tables (8 playbooks)"
```

---

### Task 6: 新建 Mesh Shader 专卡并登记索引

**Files:**
- Create: `references/03_api_manual/06_pipeline/mesh_shader.md`
- Modify: `references/03_api_manual/api_index.md`（Pipeline 分类下登记）
- Modify: `references/03_api_manual/README.md`（如该 README 列文件清单则同步；只列分类则跳过）

**Interfaces:**
- Produces: API 卡 `03_api_manual/06_pipeline/mesh_shader.md`；Task 8 的 change_log 引用此路径。

- [ ] **Step 1: 创建 `mesh_shader.md`（完整模板 §0-§15，关键节内容如下，其余按模板结构补全）**

```md
# API Card: Mesh Shader / Task Shader

## 0. Metadata

| 字段 | 内容 |
|---|---|
| 模块 | `03_api_manual` |
| 分类 | Pipeline 链 |
| Vulkan 对象 | `VkMeshShaderEXT`（feature）、`VK_STRUCTURE_TYPE_GRAPHICS_PIPELINE_CREATE_INFO`（task/mesh stage） |
| 常用 API | `vkCmdDrawMeshTasksEXT` / `vkCmdDrawMeshTasksIndirectEXT` / `vkCmdDrawMeshTasksIndirectCountEXT` |
| 适用平台 | Desktop（NVIDIA/AMD/Intel 桌面 GPU）为主；Android 移动端支持有限，需按设备查询 |
| 来源等级 | `[SPEC] [REGISTRY] [GUIDE] [ENGINE] [HEUR]` |
| 适用 Vulkan 版本 | `VK_EXT_mesh_shader`（Vulkan 1.1+，依赖 `VK_KHR_spirv_1_4`）；VK_NV_mesh_shader 为早期私有扩展 |

## 1. 一句话定位

用计算式 stage（task → mesh）替代传统 vertex/geometry 组装，配合 meshlet 化资产实现 GPU-driven 剔除与提交，减少 CPU draw call 与顶点冗余处理。

## 2. 所属对象链路

```text
VkPhysicalDeviceFeatures2（meshShader / taskShader feature 查询）
→ VkShaderModule（task / mesh / fragment）
→ VkGraphicsPipelineCreateInfo（task + mesh stage 填入，无 vertex stage）
→ vkCmdBindPipeline（VK_PIPELINE_BIND_POINT_GRAPHICS）
→ vkCmdDrawMeshTasksEXT
→ mesh shader 输出 → rasterization → fragment shader
```

### 上游依赖

- 设备 feature：`meshShader` / `taskShader`（`VkPhysicalDeviceMeshShaderFeaturesEXT`）。
- SPIR-V 1.4+（GLSL `GL_EXT_mesh_shader`）。

### 下游影响

- mesh 输出 primitives 直接送光栅化；与 fragment 后续链路兼容。
- 与 `03_api_manual/03_command_buffer/indirect_commands.md` 的 indirect draw 共享 GPU-driven 提交思想。

## 3. 核心对象与 API

### 核心 Vulkan 对象

- `VkPhysicalDeviceMeshShaderFeaturesEXT`
- `VkGraphicsPipelineCreateInfo`（pStages 含 task/mesh stage）

### 常用 API

- `vkCmdDrawMeshTasksEXT(groupCountX, groupCountY, groupCountZ)`
- `vkCmdDrawMeshTasksIndirectEXT` / `vkCmdDrawMeshTasksIndirectCountEXT`

### 相关扩展 / 版本

- `VK_EXT_mesh_shader`（跨厂商）；`VK_NV_mesh_shader`（NVIDIA 早期）。
- Android 是否可用：需查询设备能力；当前主流移动 GPU 支持不普遍，**必须运行时查询 feature 后再启用**。`[ANDROID]`

## 4. 标准使用流程

```text
查询 meshShader/taskShader feature
→ 启用 VK_EXT_mesh_shader 扩展与 feature
→ 编译 task/mesh SPIR-V（SPV_VERSION 1.4+）
→ 创建 pipeline：task(可选) + mesh + fragment，不填 vertex stage
→ meshlet 化网格资产（顶点/索引重排为 meshlet，常 64 顶点/124 三角形量级 [HEUR]）
→ vkCmdDrawMeshTasksEXT（taskCount = meshlet 分组数）
→ mesh shader 内输出最终三角形
```

## 5. 关键字段

| 字段 | 专家关注点 | 常见错误 |
|---|---|---|
| taskCount（draw 参数） | 与 mesh shader 的 local size 及 meshlet 数量匹配 | 按"三角形数"而非"task 分组数"传入 |
| `setMeshOutputsEXT` | 每次调用声明本组输出顶点/图元数 | 未调用即写输出数组 |
| mesh stage 无 vertex input | 顶点数据经 storage buffer / descriptor 读取 | 仍配置 vertex input state 导致创建失败 |
| primitive topology | mesh 输出拓扑在 shader 内声明，pipeline 填 `VK_PRIMITIVE_TOPOLOGY_...` 需与扩展要求一致（通常 mesh pipeline 忽略该字段，但部分校验器仍检查 [HEUR]） | 沿用 vertex pipeline 的 topology 语义理解 |

## 6-12. 按模板补全

正确性检查点 / 高频错误 / Debug 检查路径 / 生命周期风险 / 同步风险 / Android 注意点 / 性能注意点，要点：

- 高频错误：feature 未启用即创建 pipeline；meshlet 数据布局与 shader 读取 stride 不符；taskCount=0（同 compute groupCount=0 问题，见 `04_debug_playbooks/04_resource_sync/compute_no_output.md`）；间接 draw 的 count buffer 未同步。
- 同步风险：GPU-driven culling（task shader 内剔除后写 indirect 参数）需 compute 风格的 memory barrier 覆盖；跨 draw 依赖同 `03_api_manual/08_synchronization/pipeline_barrier.md`。
- Android 注意点：`[ANDROID]` 运行时查询 feature；不支持时回退传统 vertex pipeline + multi-draw indirect 路径（见 `03_api_manual/03_command_buffer/indirect_commands.md`）。
- 性能注意点：meshlet 大小影响顶点复用率与 task 占用率 [HEUR]（适用条件：桌面 GPU；移动端需实测）。

## 13. 专家经验

```text
经验：
mesh shader 的收益主要来自 GPU-driven culling 与 CPU draw call 卸载，而不是单三角形吞吐提升。

适用条件：
场景 draw call 数量大、CPU 提交成瓶颈、资产可离线 meshlet 化、目标平台支持扩展。

不适用情况：
目标平台不支持（多数当前 Android 设备）；资产无法 meshlet 化（程序化几何频繁变化）；draw call 数量本身不多。
```

## 14. 相关 API 卡片

- `03_api_manual/03_command_buffer/indirect_commands.md`
- `03_api_manual/06_pipeline/graphics_pipeline.md`
- `03_api_manual/06_pipeline/shader_module.md`
- `03_api_manual/05_descriptor/storage_buffer_image_descriptor.md`

## 15. 需要回查官方文档的情况

1. `VK_EXT_mesh_shader` 在目标设备的支持情况。
2. taskCount / meshlet 输出上限（`maxMeshWorkGroupTotalCount` 等 limit）。
3. 与其他 stage/feature 的交互限制。
```

- [ ] **Step 2: 在 `api_index.md` 的 Pipeline 分类下登记**

在 pipeline 相关条目后加：

```md
- `06_pipeline/mesh_shader.md` — Mesh/Task Shader、meshlet、GPU-driven 提交（VK_EXT_mesh_shader）
```

- [ ] **Step 3: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add references/03_api_manual/06_pipeline/mesh_shader.md references/03_api_manual/api_index.md
git commit -m "Add mesh shader API card (VK_EXT_mesh_shader) and index entry"
```

---

### Task 7: 新建桌面窗口集成专卡并登记索引

**Files:**
- Create: `references/03_api_manual/02_surface_swapchain/desktop_windowing.md`
- Modify: `references/03_api_manual/api_index.md`（Surface/Swapchain 分类下登记）

**Interfaces:**
- Produces: API 卡 `03_api_manual/02_surface_swapchain/desktop_windowing.md`；Task 8 的 change_log 引用此路径。

- [ ] **Step 1: 创建 `desktop_windowing.md`（完整模板，关键节内容如下）**

```md
# API Card: Desktop Windowing Integration（SDL / GLFW / Win32）

## 0. Metadata

| 字段 | 内容 |
|---|---|
| 模块 | `03_api_manual` |
| 分类 | 显示链（Surface/Swapchain）+ Desktop 平台 |
| Vulkan 对象 | `VkSurfaceKHR` / `VkSwapchainKHR` / `VkExtent2D` |
| 常用 API | `vkCreateWin32SurfaceKHR` / `vkCreateSwapchainKHR` / `vkAcquireNextImageKHR` / `vkQueuePresentKHR` |
| 适用平台 | Windows（Win32 / SDL2 / GLFW3）；Linux/macOS 思路一致但窗口 API 不同 |
| 来源等级 | `[SPEC] [GUIDE] [ENGINE] [HEUR]` |
| 适用 Vulkan 版本 | Vulkan 1.x + `VK_KHR_surface` / `VK_KHR_win32_surface`（多数为 instance 扩展，1.0 可用） |

## 1. 一句话定位

把桌面窗口系统的窗口句柄桥接为 `VkSurfaceKHR`，并处理桌面特有事件（最小化 / resize / DPI 变化）下的 swapchain 生命周期，是 Android Surface 生命周期问题的桌面对等物。

## 2. 所属对象链路

```text
窗口句柄（HWND / SDL_Window / GLFWwindow）
→ vkCreateWin32SurfaceKHR（或 SDL_Vulkan_CreateSurface / glfwCreateWindowSurface）
→ vkGetPhysicalDeviceSurfaceSupportKHR（选 present queue）
→ vkGetPhysicalDeviceSurfaceCapabilitiesKHR（当前 extent）
→ vkCreateSwapchainKHR
→ acquire / submit / present 循环
```

### 上游依赖

- instance 扩展：`VK_KHR_surface` + `VK_KHR_win32_surface`（SDL/GLFW 的封装函数会自动启用）。

### 下游影响

- 窗口事件（resize / 最小化 / DPI）直接映射为 swapchain 依赖资源组重建，规则与 Android rotation 相同，见 `02_core_mental_model/android_surface_swapchain_lifecycle.md` 的桌面镜像一节。

## 3. 核心对象与 API

### 常用 API

- 原生：`vkCreateWin32SurfaceKHR`（需 `VkWin32SurfaceCreateInfoKHR` 的 hwnd/hinstance）。
- SDL2：`SDL_Vulkan_CreateSurface`（SDL ≥ 2.0.6；SDL3 为 `SDL_Vulkan_CreateSurface` 签名有变，使用前核对版本 [TOOL]）。
- GLFW3：`glfwCreateWindowSurface`。
- 能力查询：`vkGetPhysicalDeviceSurfaceCapabilitiesKHR` / `vkGetPhysicalDeviceSurfaceFormatsKHR` / `vkGetPhysicalDeviceSurfacePresentModesKHR`。

## 4. 标准使用流程

```text
创建窗口（不自动创建 OpenGL 上下文）
→ 创建 instance 并启用 surface 扩展
→ 创建 VkSurfaceKHR（库封装或原生 API）
→ 选 present queue（surfaceSupport 检查）
→ 查询 surface capabilities / formats / present modes
→ 创建 swapchain（extent 取 clamp(capabilities.currentExtent)）
→ 渲染循环：acquire → submit（含 present/wait semaphore）→ present
→ 处理窗口事件 → 必要时 recreate swapchain
```

## 5. 关键字段

| 字段 | 专家关注点 | 常见错误 |
|---|---|---|
| `capabilities.currentExtent` | 为 0xFFFFFFFFFFFFFFFF 时需用窗口实际像素尺寸 | 直接用窗口逻辑尺寸（DPI 缩放后不一致） |
| `imageExtent` | 必须在 min/max extent 范围内 clamp | 未 clamp 直接用窗口尺寸 |
| `clipped` | 通常 VK_TRUE（被遮挡区域不保证内容） | 期望遮挡区仍可读回时误开 clipped |
| present mode | FIFO 保底可用；MAILBOX/IMMEDIATE 视平台 | 未确认支持即请求 MAILBOX |

## 6-12. 按模板补全，要点：

- 高频错误：最小化后仍持续 present（应暂停渲染循环，等 restore）；resize 消息风暴中每帧 recreate（应去抖或按 acquire 返回错误触发）；DPI 缩放下用逻辑坐标当像素坐标；`VK_ERROR_OUT_OF_DATE_KHR` / `VK_SUBOPTIMAL_KHR` 处理遗漏（前者必须 recreate，后者可延迟到帧末）。
- 生命周期风险：窗口销毁前必须销毁 swapchain 与 surface（依赖逆序）；事件回调里同步销毁 Vulkan 对象与 render thread 竞争（需暂停渲染线程后处理）。
- 同步风险：acquire signal semaphore 与 present wait semaphore 的配对，与 `03_api_manual/03_command_buffer/fence_semaphore.md` 相同。
- Android 注意点：不适用（本卡为桌面平台）；Android 对等卡见 `03_api_manual/01_instance_device_queue/` 下 Android 平台链与 `02_core_mental_model/android_surface_swapchain_lifecycle.md`。
- 性能注意点：窗口模式 vs 全屏独占的 present mode 与 tearing 行为 [HEUR]；HDR/可变刷新率需查询扩展支持后启用。

## 13. 专家经验

```text
经验：
桌面 swapchain recreate 的触发策略优先"由 acquire/present 返回码驱动"，而不是仅依赖窗口事件；事件只作提前提示。

适用条件：
窗口系统事件可能丢失或延迟（远程桌面、合成器行为差异）的多平台桌面应用。

不适用情况：
事件可靠且需要即时响应的单一平台应用（如仅 Windows 原生），可只监听消息循环。
```

## 14. 相关 API 卡片

- `03_api_manual/02_surface_swapchain/swapchain.md`
- `03_api_manual/02_surface_swapchain/swapchain_recreate.md`
- `03_api_manual/03_command_buffer/fence_semaphore.md`
- `03_api_manual/03_command_buffer/frames_in_flight.md`

## 15. 需要回查官方文档的情况

1. SDL/GLFW 封装函数在所用版本的确切签名（版本差异大 [TOOL]）。
2. 各平台 surface 扩展（win32/xlib/wayland/metal）的字段要求。
3. present mode / composite alpha 在目标合成器的实际支持。
```

- [ ] **Step 2: 在 `api_index.md` Surface/Swapchain 分类下登记**

```md
- `02_surface_swapchain/desktop_windowing.md` — SDL/GLFW/Win32 桌面窗口集成、最小化/resize/DPI 与 swapchain 生命周期
```

- [ ] **Step 3: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add references/03_api_manual/02_surface_swapchain/desktop_windowing.md references/03_api_manual/api_index.md
git commit -m "Add desktop windowing API card (SDL/GLFW/Win32) and index entry"
```

---

### Task 8: 入口与路由接入

**Files:**
- Modify: `SKILL.md`（启动加载顺序 + 回答规则）
- Modify: `references/00_expert_entry/task_classifier.md`（跨类型任务规则）
- Modify: `references/07_integration_pack/retrieval_policy.md`（§2 降级为首轮路由）
- Modify: `references/00_expert_entry/response_formats.md`（新增 §0.5 意图澄清格式）
- Modify: `references/00_expert_entry/verification_gate.md`（交叉引用）
- Modify: `references/MODULE_SUMMARY.md`（00_expert_entry 10→11、03_api_manual 66→67）
- Modify: `references/00_expert_entry/change_log.md`（v1.0.5 条目）

**Interfaces:**
- Consumes: `00_expert_entry/progressive_retrieval.md`（Task 1）、两张新卡（Task 6/7）。

- [ ] **Step 1: `SKILL.md` 两处修改**

启动加载顺序第 6 项后加：

```md
7. `references/00_expert_entry/progressive_retrieval.md`
```

回答规则段（"修改类任务…regression_checklist.md"段落之后）加：

```md
复杂、跨类型或信息不足的任务，按 `references/00_expert_entry/progressive_retrieval.md` 先执行检索循环：跨类型任务先查询分解再逐子问题路由；信息不足时先给最小提问清单（不超过 3 项）再路由；回答前对每个子问题标注置信度，低置信子问题必须二轮定向补载。
```

- [ ] **Step 2: `task_classifier.md` 分类规则段加跨类型处理**

在"跨类型时按固定优先级处理主类型"之后补充：

```md
跨类型任务先用 `00_expert_entry/progressive_retrieval.md` 的 R1 查询分解拆为子问题，再对每个子问题独立分类与路由；只有子问题无法拆分（单一问题天然横跨多类，如"迁移 OpenGL 项目到 Vulkan 并优化性能"）时才用固定优先级取主类型。
```

- [ ] **Step 3: `retrieval_policy.md` §2 开头加说明**

在 "## 2. 按任务逐步检索" 标题后加：

```md
本节的单一任务路径是检索循环的**首轮路由**（R3）。复杂、跨类型或信息不足的任务，按 `00_expert_entry/progressive_retrieval.md` 执行完整循环（R1 分解 → R2 澄清 → R3 路由 → R4 评估 → R5 二轮补载）。
```

- [ ] **Step 4: `response_formats.md` 在 §0 之后加 §0.5**

```md
## 0.5 意图澄清（信息不足时，先于任何类型化输出）

触发判据与提问规则见 `00_expert_entry/progressive_retrieval.md` R2。输出格式：

```text
意图澄清（当前信息不足以可靠路由，需要以下证据）：
1. <平台 / Vulkan 版本 / 设备>（用于平台分支路由）
2. <现象细节：黑屏 / 闪烁 / 花屏 / 拉伸 / 其他>（用于症状分支路由）
3. <Validation 报错 / VUID / logcat / 返回值，任一>（用于根因表定位）
```

规则：每项必须"补齐即可路由"；一次 ≤3 项；用户无法提供时按启发式降级路由并在回答中标注假设。
```

- [ ] **Step 5: `verification_gate.md` 加交叉引用**

在文件末尾（或 G6 之后）加：

```md
输入来源：进入 G1-G6 前，证据清单与各子问题置信度来自 `00_expert_entry/progressive_retrieval.md` 的检索循环输出；检索循环未达成高置信的子问题，在对应关卡标注"未验证"及原因。
```

- [ ] **Step 6: `MODULE_SUMMARY.md` 更新两行**

```md
- `00_expert_entry`: 11 files
- `03_api_manual`: 67 files
```

- [ ] **Step 7: `change_log.md` 在 v1.0.4 条目之前加 v1.0.5 条目**

```md
## v1.0.5

### Intelligent Retrieval：检索循环 + 证据驱动决策树 + 领域补白

新增 `00_expert_entry/progressive_retrieval.md`：五阶段检索循环（R1 查询分解 / R2 意图澄清 / R3 首轮路由 / R4 证据评估 / R5 二轮定向检索）；复杂跨类任务先分解再路由，模糊提问先给 ≤3 项最小提问清单，低置信子问题强制二轮补载；循环输出作为 Verification Gate 输入。

Debug Playbook §3 全量决策表化（18 个 playbook 文件 / 24 个小节）：快速验证路径从线性步骤改为"检查（成本升序）→ 结果分支 → 剪枝（引用 §2 假设编号）"的证据驱动决策表；模板与 README 同步新规范。

新增 API 卡片：`03_api_manual/06_pipeline/mesh_shader.md`（VK_EXT_mesh_shader、meshlet、GPU-driven，Android 支持边界显式标注）与 `03_api_manual/02_surface_swapchain/desktop_windowing.md`（SDL/GLFW/Win32、最小化/resize/DPI 与 swapchain 生命周期）。

入口与路由接入：`../../SKILL.md` 启动加载第 7 项与回答规则；`00_expert_entry/task_classifier.md` 跨类型先分解；`07_integration_pack/retrieval_policy.md` §2 降级为首轮路由；`00_expert_entry/response_formats.md` 新增 §0.5 意图澄清格式；`00_expert_entry/verification_gate.md` 交叉引用。索引同步：api_index、MODULE_SUMMARY（00_expert_entry 10→11、03_api_manual 66→67）。
```

- [ ] **Step 8: `SKILL.md` metadata 版本更新**

```yaml
  version: 1.0.5
  last-updated: '2026-08-29'
```

- [ ] **Step 9: 运行验证**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

Run: `git grep -c "progressive_retrieval.md" -- SKILL.md references/`
Expected: ≥6 行（SKILL.md、task_classifier、retrieval_policy、response_formats、verification_gate、change_log）

- [ ] **Step 10: Commit**

```bash
git add SKILL.md references/
git commit -m "Wire progressive retrieval into entry rules, routing, response formats, and changelog for v1.0.5"
```

---

### Task 9: 最终回归与验收

**Files:**
- 无新文件；只做验证。

- [ ] **Step 1: 文件数验收**

Run: `git ls-files | Measure-Object -Line`
Expected: `199`（≤200）

- [ ] **Step 2: 测试与行尾**

Run: `python -m pytest tests/ -q`
Expected: `64 passed, 1 skipped`

Run: `git diff --check`
Expected: 无输出

- [ ] **Step 3: 决策表覆盖验收**

Run: `git grep -c "快速验证路径（证据驱动决策表）" -- references/04_debug_playbooks/`
Expected: 25 行总计（24 个小节 + 模板 1）

Run: `git grep -n "3\. 快速验证路径$" -- references/04_debug_playbooks/`
Expected: 无输出（旧标题无残留；匹配任意层级）

- [ ] **Step 4: 新卡登记验收**

Run: `git grep -n "mesh_shader.md\|desktop_windowing.md" -- references/03_api_manual/api_index.md`
Expected: 2 行

- [ ] **Step 5: 剪枝列规范抽查**

Run: `git grep -c "剪枝（排除的假设）" -- references/04_debug_playbooks/`
Expected: ≥25 行（24 小节 + 模板 1）

- [ ] **Step 6: 汇总报告**

向用户输出：修改文件列表、每项修改解决什么问题、v1.0.5 新增专家行为、与 v1.0.4 的行为差异、5 个回归测试问题（见设计文档 §6）。等待用户确认后再提交推送。
